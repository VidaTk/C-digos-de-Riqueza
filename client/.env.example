# Local dev: deja esto vacío/sin definir, el proxy de Vite se encarga (ver vite.config.ts).
# En Vercel: pon la URL completa del backend desplegado, sin slash final.
# VITE_API_BASE_URL="https://codigos-riqueza-api.vercel.app"
